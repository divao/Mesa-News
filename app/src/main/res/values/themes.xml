<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MesaNews" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/primaryLightTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorOnSecondary">@color/secondaryLightTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/primaryLightTextColor</item>
        <item name="android:toolbarStyle">@style/Theme.MesaNews.ToolbarTheme</item>
        <item name="imageButtonStyle">@style/Theme.MesaNews.ImageButtonTheme</item>
    </style>

    <style name="Theme.MesaNews.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MesaNews.ToolbarTheme">
        <item name="titleTextColor">@color/primaryLightTextColor</item>
        <item name="android:textColor">@color/primaryLightTextColor</item>
    </style>

    <style name="Theme.MesaNews.ImageButtonTheme">
        <item name="tint">@color/primaryLightTextColor</item>
    </style>

    <style name="Theme.MesaNews.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>